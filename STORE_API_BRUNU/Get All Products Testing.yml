info:
  name: Get All Products Testing
  type: http
  seq: 2

http:
  method: GET
  url: "{{store_api}}/static"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
